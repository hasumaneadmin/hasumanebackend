# Nixpacks configuration for HasuMane backend and frontend proxy
[variables]
NIXPACKS_NODE_VERSION = "22"

[phases.build]
cmds = ["npm ci", "npx prisma generate", "npm run build"]

[start]
cmd = "node scripts/start-production.js"
