# HasuMane Frontend Environment Variables

# The base URL of the NestJS backend. Client code appends /api/v1.
BACKEND_API_URL=http://localhost:5000

# Browser-side backend origin for the admin panel. Client code appends /api/v1.
VITE_API_URL=http://localhost:5000
